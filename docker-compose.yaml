version: '3.9'
services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: saadia
      POSTGRES_PASSWORD: password
      POSTGRES_USER: user
    volumes:
      - .dbdata:/var/lib/postgres
    ports:
      - "5432:5432"

  # redis:
  #   image: redis
  #   ports:
  #     - "6379:6379"